<%= form_with model: current_user.active_relationships.find_by(followed_id:@user.id),html:{method: :delete} ,local: true do |form|%>
  <%= form.submit "Unfollow", class:"btn btn-primary" %>
<% end %>
