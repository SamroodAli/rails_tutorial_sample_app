<%= form_with model: current_user.active_relationships.find_by(followed_id:@user.id),remote: true,html:{method: :delete} do |form|%>
  <%= form.submit "Unfollow", class:"btn btn-primary" %>
<% end %>
